# 007 Build Virtual Team and Role System

Implement built-in roles, default providers, default virtual agents, and room profile persistence. Support custom roles, editing custom roles, deleting custom roles, assigning multiple roles per agent, assigning one role to many agents, export, import, and restore defaults.

Tests must prove Atlas/Forge share Claude, Sentinel/Gauge share Codex, Scout is disabled by default, and role assignments persist.
