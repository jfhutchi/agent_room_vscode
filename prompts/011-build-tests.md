# 011 Build Tests

Use Node's built-in test runner for core modules. Cover PromptBuilder, RoleRegistry, VirtualTeamRegistry, WorkflowRunner, ModelAdvisor, TranscriptStore, CLI parsing, SafetyPolicy, child process behavior, webview message validation, and command registration surface.

Run `npm test`, `npm run compile`, and `npm run lint`; fix failures before final review.
